import { clearStore, test, assert } from "matchstick-as/assembly/index";

import { handleDepositMinted } from "../src/mapping";

export function runTests(): void {
  test("Can call mappings with custom events", () => {
    console.log("asd");
  });
}
